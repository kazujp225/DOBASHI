databases:
  - name: tora-db
    plan: free
    databaseName: tora_analysis
    user: tora_user

services:
  - type: web
    name: tora-app
    runtime: docker
    region: singapore
    plan: free
    repo: https://github.com/kazujp225/DOBASHI
    branch: main
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tora-db
          property: connectionString
      - key: DATABASE_TYPE
        value: postgresql
      - key: YOUTUBE_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11.0"
