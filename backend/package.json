{
  "name": "tora-backend",
  "version": "2.9.1",
  "description": "令和の虎 コメント分析システム - バックエンドAPI",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "init-db": "python scripts/init_db.py"
  }
}
